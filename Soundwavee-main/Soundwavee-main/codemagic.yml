workflows:
  capacitor-android:
    name: Build Android APK
    environment:
      node: 18
    scripts:
      - npm install
      - npm run build
      - npx cap sync android
      - cd android
      - ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/**/*.apk
